{
  "name": "Scarper bot",
  "description": "get your APP ID and API HASH inside Telegram (Python-Pyrogram)",
  "keywords": [
    "telegram",
    "app",
    "scrapper",
    "python",
    "pyrogram",
    "api_id",
    "api_hash"
  ],
  "repository": "https://github.com/Kobsser/appScraper",
  "success_url": "https://t.me/Kobsser",
  "website": "https://github.com/Kobsser/appScraper",
  "env": {
    "TG_BOT_TOKEN": {
      "description": "Your bot token, as a string."
    },
    "TG_BOT_USERNAME": {
      "description": "Your Bot Username, as a string."
    },
    "LOG_CHANNEL_ID": {
      "description": "create a private channel and then put its id (for send Errors)"
    },
    "BOT_API_ID": {
      "description": "Bot needed api id & hash for working"
    },
    "BOT_API_HASH": {
      "description": "."
    },
    "APP_TITLE": {
      "description": "specify a custom app title",
      "required": false
    },
    "APP_SHORT_NAME": {
      "description": "specify a custom alphamumeric shortname (5-32 characters)",
      "required": false
    },
    "APP_URL": {
      "description": "specify a custom app URL",
      "required": false
    },
    "APP_DESCRIPTION": {
      "description": "specify a custom app description",
      "required": false
    },
    "FTEXT": {
      "description": "the footer text that will be seen in the bot message, after the final edit",
      "required": false
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "free"
    }
  },
  "stack": "heroku-20"
}
